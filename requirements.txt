# Runtime dependencies for the MCP Tool-Retrieval Gateway.

fastapi>=0.110          # HTTP + JSON-RPC surface
uvicorn[standard]>=0.29 # ASGI server to run the app
numpy>=1.24             # cosine vector store
pyyaml>=6.0             # YAML config parsing
mcp>=2.0                # official MCP SDK: real stdio client + server transport

# --- Dev/test only (installed for running the suite) ---
pytest>=8.0
httpx>=0.27             # required by fastapi.testclient

# --- Default semantic embedder (v0.2) ---
# As of v0.2 the default embedder is sentence-transformers (all-MiniLM-L6-v2),
# which downloads ~80MB on first use. Install it for semantic retrieval:
#     pip install sentence-transformers
# ...or run fully offline/deterministic with the hashing fallback:
#     export MCP_ROUTER_EMBEDDER=hashing
# sentence-transformers>=2.2
